#head.page.title
  %h1= @book.title
  %h2= @book.subtitle
  // = "(#{@book.text.language})"
  by
  %h3= @book.author
  %h3{ :onclick => 'alert("email");' } ✏
.shadow

.page
  .text= @book.text
  :javascript
    $(function() {
      splitPages();
    });

.add_more
  = link_to '+', '#'

#tail.page.title
  %h2 The End
  %h3 ❦
  // ⊛
  // ∞
  // ∗
.shadow
  
.options-trigger= link_to_function 'Opt', 'showOptions()'
#options
  .pdf= link_to 'd/l', pdf_path

%ol.chapternav
  %li ch.
  %li.current 1
  %li
    = link_to 'title', '#head'